use anyhow::Result;

/// Verify sanitization (placeholder)
pub fn verify_device(device: &str) -> Result<bool> {
    println!("Verifying device: {}", device);
    // Later: sample random blocks
    Ok(true)
}
